<Page x:Class="CinemaApp.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Главная">
    <Grid Margin="10" Background="LightCyan">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">

            <TextBlock Text="Кинотеатр Звезда" FontSize="20" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button x:Name="BtnProfile" Content="Личный кабинет" 
                    Padding="10,5" Click="BtnProfile_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="TbSearch" Width="200" Height="25"
                     TextChanged="TbSearch_TextChanged"/>
            <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="15,0,5,0"/>
            <ComboBox x:Name="CbSort" Width="150" Height="25"
                      SelectionChanged="CbSort_SelectionChanged">
                <ComboBoxItem Content="Без сортировки" IsSelected="True"/>
                <ComboBoxItem Content="По названию"/>
                <ComboBoxItem Content="По рейтингу"/>
            </ComboBox>
        </StackPanel>

        <ListBox x:Name="LbFilms" Grid.Row="2"
                 MouseDoubleClick="LbFilms_MouseDoubleClick">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" 
                            Padding="10" Margin="5" Width="180">
                        <StackPanel>
                            <Image Source="{Binding PosterPath}" Width="150"/>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold"
                                       TextWrapping="Wrap"/>
                            <TextBlock>
                                <Run Text="Рейтинг: "/>
                                <Run Text="{Binding Rating, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding AgeRating}"/>
                            <TextBlock Text="{Binding ReleaseDate, StringFormat='Дата: {0:dd.MM.yyyy}'}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
